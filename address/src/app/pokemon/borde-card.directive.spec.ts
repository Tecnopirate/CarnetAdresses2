import { BordeCardDirective } from '../borde-card.directive';

describe('BordeCardDirective', () => {
  it('should create an instance', () => {
    //const directive = new BordeCardDirective();
    //expect(directive).toBeTruthy();
  });
});
